# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

name: PR-XPU

on:
  push:
    branches:
      - "pull-request/[0-9]+"

concurrency:
  group: pr-xpu-${{ github.ref_name }}
  cancel-in-progress: true

jobs:

  # ============================================================================
  # GATE – only run XPU CI when the PR has the "xpu" label.
  # Push trigger (pull-request/* branch) already guarantees only internal
  # users / copy-pr-bot can reach this workflow.
  # ============================================================================

  guard:
    runs-on: ubuntu-latest
    outputs:
      has_xpu_label: ${{ steps.pr-info.outputs.has_xpu_label }}
    steps:
      - name: Get PR info and check xpu label
        id: pr-info
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          PR_NUMBER="${GITHUB_REF_NAME##*/}"

          # Check for xpu label
          HAS_XPU=$(gh api "repos/${GITHUB_REPOSITORY}/pulls/${PR_NUMBER}" \
            --jq '[.labels[].name] | any(. == "xpu")')
          echo "has_xpu_label=${HAS_XPU}" >> "$GITHUB_OUTPUT"
          if [[ "${HAS_XPU}" == "true" ]]; then
            echo "✅ PR #${PR_NUMBER} has 'xpu' label – running XPU CI"
          else
            echo "⏭️ PR #${PR_NUMBER} does not have 'xpu' label – skipping XPU CI"
          fi

  # ============================================================================
  # CHANGE DETECTION
  # ============================================================================

  changed-files:
    needs: [guard]
    if: needs.guard.outputs.has_xpu_label == 'true'
    runs-on: ubuntu-latest
    outputs:
      core: ${{ steps.changes.outputs.core }}
      vllm: ${{ steps.changes.outputs.vllm }}
      sglang: ${{ steps.changes.outputs.sglang }}
    steps:
      - name: Checkout code
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955  # v4.3.0
        with:
          fetch-depth: 0
      - name: Check for changes
        id: changes
        uses: ./.github/actions/changed-files
        with:
          gh_token: ${{ github.token }}

  # ============================================================================
  # XPU CI – VLLM
  # ============================================================================

  xpu-ci-vllm:
    needs: [changed-files]
    if: >-
      needs.changed-files.outputs.core == 'true' ||
      needs.changed-files.outputs.vllm == 'true'
    uses: ./.github/workflows/xpu-ci.yaml
    with:
      framework: vllm
      pytest_markers: 'vllm and xpu_1 and pre_merge'

  # ============================================================================
  # STATUS CHECK
  # ============================================================================

  xpu-status-check:
    runs-on: ubuntu-latest
    needs: [guard, changed-files, xpu-ci-vllm]
    if: always() && needs.guard.outputs.has_xpu_label == 'true'
    steps:
      - name: Check all XPU jobs
        run: |
          echo '${{ toJson(needs) }}' | jq -e 'to_entries | map(.value.result) | all(. as $result | ["success", "skipped"] | any($result == .))'
